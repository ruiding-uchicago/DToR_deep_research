# Requirements for FET RAG system only
# These are the minimal requirements to run the advanced FET sensor RAG
# without needing the full deep research agent dependencies

# Core vector store
chromadb>=0.4.0
langchain-chroma>=0.1.0

# Embeddings
langchain-huggingface>=0.0.1
sentence-transformers>=2.0.0
transformers>=4.30.0

# Core langchain (needed for base classes)
langchain>=0.1.0
langchain-community>=0.0.1
langchain-core>=0.1.0

# Utilities
numpy>=1.20.0
pandas>=1.3.0

# Optional but recommended
tqdm>=4.60.0